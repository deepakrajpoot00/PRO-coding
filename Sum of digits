#include <stdio.h>

int main() {
    int n,s;
    int sum = 0;
    printf("Enter a number : ");
    scanf("%d", &n);
    
    while(n != 0){
        s = n % 10;
        sum = sum + s;
        n = n / 10;
    }
    printf("the sum of digits = %d", sum);
    
    return 0;
}
