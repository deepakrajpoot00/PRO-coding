#include <stdio.h>

int main() {
    int n,s;
    int r = 0;
    printf("Enter a number : ");
    scanf("%d", &n);
    
    while(n != 0){
        r = r * 10 + n % 10;
        n = n / 10;
    }
    printf("Reverse = %d", r);
    
    return 0;
}
